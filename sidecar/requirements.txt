# gym_sidecar.py (real-physics PushT testing backend)
gymnasium
gym-pusht
pymunk>=6.2,<7   # gym-pusht 0.1.6 uses the pymunk 6.x collision-handler API;
                # pymunk 7 removed add_collision_handler and breaks the env.
pillow
numpy

# run_policy only — pretrained LeRobot policies. Heavier; pulls torch.
# lerobot

# sim_sidecar.py needs nothing (Python standard library only).
# A2A card signing is handled by Lex (std.crypto); the sidecar just stores
# and serves the pre-signed blobs pushed by register_cards (a2a_server.lex).

# depot_mujoco_sidecar.py (Tier-2 MuJoCo depot scene)
# xlerobot_mujoco_sidecar.py + gym_env/xlerobot_env.py (XLeRobot room scene;
# the gym env additionally uses gymnasium, listed above).
# xlerobot_sidecar.py (Tier-1 stub) needs nothing -- Python stdlib only.
mujoco

# xlerobot_sidecar.py Tier-3 (LEX_ROBOT_HW=1, real XLeRobot hardware) — see
# SIDECAR.md "Real hardware" for the env vars and bring-up steps. Not needed
# for Tier-1/Tier-2 and not installed in CI (no hardware to test against).
#   pip install "lerobot[feetech]"    # SO101Follower + FeetechMotorsBus (arms + diff base)
#   pip install "lerobot[kinematics]" # placo -- move_arm's Cartesian IK/FK (needs
#                                      # LEX_XLE_URDF_PATH too, see SIDECAR.md; lerobot no
#                                      # longer builds this into the robot object for you).
#                                      # No URDF ships with lerobot/placo -- sparse-clone
#                                      # Simulation/SO101/so101_new_calib.urdf from
#                                      # https://github.com/TheRobotStudio/SO-ARM100 (bench-
#                                      # tested working; see SIDECAR.md "Real hardware" for
#                                      # the clone command).
#   pip install sounddevice           # mic capture for `listen`, speaker playback for `speak`
#   pip install faster-whisper        # local speech-to-text for `listen`
#   pip install kokoro                # local text-to-speech for `speak` (hexgrad/Kokoro-82M;
#                                      # pulls torch + transformers -- heavier, see below)
#   pip install pillow                # JPEG-encodes read_camera frames (also listed above)
#   pip install "qrcode[pil]"         # render_qr's QR image (scan_qr needs nothing extra --
#                                      # cv2 ships as a base "lerobot[feetech]" dependency)

# depot_g1_sidecar.py (Tier-3: real Unitree G1 humanoid) also needs the G1 model
# from MuJoCo Menagerie (heavy STL meshes, not vendored). Sparse-checkout it and
# point LEX_G1_DIR at it:
#   git clone --depth 1 --filter=blob:none --sparse \
#     https://github.com/google-deepmind/mujoco_menagerie.git /tmp/menagerie
#   git -C /tmp/menagerie sparse-checkout set unitree_g1
#   export LEX_G1_DIR=/tmp/menagerie/unitree_g1

# g1_bc_reach.py (learned reach policy via behavior cloning) also needs torch:
#   pip install torch    # MPS-accelerated on Apple Silicon

# xlerobot_rl_train.py + gym_env/xlerobot_rl_eval.py (RL training loop against
# LexXLeRobotFetch-v0 — the mujoco/gymnasium deps above, plus:
#   pip install stable-baselines3    # PPO; pulls torch
# xlerobot_rl_finetune.py (retrain from a real rollout's usage log against
# gym_env/xlerobot_governed_env.py's LexXLeRobotFetchGoverned-v0) needs the
# same stable-baselines3. gym_env/xlerobot_usage_log.py needs nothing extra
# (Python stdlib only).
